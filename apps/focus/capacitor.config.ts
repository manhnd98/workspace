import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.focus',
  appName: 'focus',
  webDir: '../../dist/apps/focus',
  bundledWebRuntime: false,
};

export default config;
