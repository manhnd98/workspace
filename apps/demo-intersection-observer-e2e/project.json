{
  "root": "apps/demo-intersection-observer-e2e",
  "sourceRoot": "apps/demo-intersection-observer-e2e/src",
  "projectType": "application",
  "targets": {
    "e2e": {
      "executor": "@nrwl/cypress:cypress",
      "options": {
        "cypressConfig": "apps/demo-intersection-observer-e2e/cypress.json",
        "devServerTarget": "demo-intersection-observer:serve:development"
      },
      "configurations": {
        "production": {
          "devServerTarget": "demo-intersection-observer:serve:production"
        }
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "apps/demo-intersection-observer-e2e/**/*.{js,ts}"
        ]
      }
    }
  },
  "tags": [],
  "implicitDependencies": [
    "demo-intersection-observer"
  ]
}
